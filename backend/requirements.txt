# Pet Management System — backend dependencies
# Pinned to compatible major/minor lines per the approved architecture (§5).

fastapi==0.115.6
uvicorn[standard]==0.34.0

# ORM + async MySQL driver
SQLAlchemy==2.0.36
# aiomysql is pinned; if it becomes unmaintained, migrate to asyncmy (drop-in replacement).
aiomysql==0.2.0

# Migrations
alembic==1.14.0

# Validation / settings
pydantic==2.10.4
pydantic-settings==2.7.1
email-validator==2.2.0

# Testing
pytest==8.3.4
pytest-asyncio==0.25.2
httpx==0.28.1
aiosqlite==0.20.0
